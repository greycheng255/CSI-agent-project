# 后端 Dockerfile - 最小化构建，使用预装依赖的基础镜像
FROM node:20

WORKDIR /app

# 设置环境变量
ENV NODE_ENV=production

# 复制所有依赖和构建产物
COPY package*.json ./
COPY node_modules ./node_modules
COPY dist ./dist

# 创建数据目录
RUN mkdir -p /data

# 暴露端口
EXPOSE 4000

# 启动命令
CMD ["node", "dist/main.js"]
