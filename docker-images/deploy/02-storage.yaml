# ============================================
# 持久卷 - 上传文件存储
# ============================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: genesis-uploads-pvc
  namespace: genesis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
