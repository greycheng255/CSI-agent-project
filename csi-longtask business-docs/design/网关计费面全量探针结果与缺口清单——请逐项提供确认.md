> 依据：我方对网关面 12 个契约端点的全量真实探针（evidence 见 08-28-csi-gateway-integration/evidence-gateway-probe-2026-09-07.md）。

主题：网关计费面全量探针结果与缺口清单——请逐项提供/确认，收齐即启阶段 1 冒烟**

Marketplace 团队各位好：

为避免逐项信函往返，我方已按契约端点清单对网关计费面做了一轮**全量真实探针**（12 个端点 + 对照组，带同构 HMAC 签名）。结果与缺口如下，请贵方逐项提供或确认——全部就绪后我方当天即可启动阶段 1 冒烟（我方客户端代码已全部就绪，含刚完成的 E4 增量游标对齐）。

**一、探针结果总览**

1. **贵方 09-07 回函提供的沙箱地址 `http://122.51.51.177:4001/v1` 实测不可达**——从我方开发机（外部源）与 122 主机自身连接均超时，与贵方回函"4001 公网已通（贵方 pull 在用）"的表述不符（我方 pull 实际走同机内网 `172.17.0.14:4001`，从不经公网）；经我方 Owner 侧确认，公网 4001 云服务器防火墙未开放——见 G2；
2. 我方按契约端点清单做了一轮**全量真实探针**（12 个端点 + 对照组，带同构 HMAC 签名），并对贵方所在 docker 网段（172.17.0.x）做了开放端口扫描与逐服务探测定性——**网段内全部存活 HTTP 服务（marketplace 4001/4000、casdoor、Next.js 前端、Docker Registry ×2、nginx 静态站、Go 服务 ×2）无一注册契约端点**：E1 套餐 / E2 目录 / E3 额度 / E4 用量 / E5 能力 / E6 免费额度激活 / L1 chat / L2 embeddings / L3 models / K1 签发 / K3 吊销全部 404（路由未注册）；
3. 附带发现：`GET /v1/entitlement/llm-config/:orgId`（E7）**已实现在 marketplace 服务上**（路由存在，当前返 500 内部错误）——与我方"E7 随 BYOK 冻结不接入"口径无冲突，仅请知悉修复 500 与否由贵方定。

**二、缺口清单（请逐项回复）**

| # | 项 | 需要什么 |
|---|---|---|
| G1 | **网关服务真实部署地址**（**首要缺口**） | docker 网段全扫描未发现任何契约端点——12 端点（E1-E6/L1-L3/K1/K3）实际部署在哪（内网 IP:端口即可）？若网关部署在其它主机/网段，请提供地址；若尚未部署，请给排期 |
| G2 | **公网 4001 连通性** | 贵方回函称"公网已通"，实测防火墙未开放——若联调需公网请开放；若仅走内网请明示并更正回函表述 |
| G3 | **测试凭证** | "加密渠道另发"至今未收到——服务间 HMAC 四件套的 token/secret 请经联调群加密文件提供；并确认是否复用长任务 c2m- 方向对 |
| G4 | **org_id 测试值** | E1-E3/E6 入参的联调期约定值（D1 双 org：一个有套餐一个无套餐） |
| G5 | **D1-D6 测试数据** | 预置完成时间（阶段 3 前需要，阶段 1 冒烟至少需 D1/D2） |
| G6 | **《CSI-LLM-Gateway-Billing-Team-Requirements.md》§6 回写区** | 正式回写文档（09-07 承诺随函回传，尚未收到）——**再次提醒**：该文档 §6"网关团队回写区"（沙箱/生产 base URL、服务间认证与凭证分发、接口路径与字段映射、org_id 测试值约定、计价币种与精度、E4 拉取形态、K1-K4 key 体系、错误结构与 code 清单、D1-D6 预置、联调窗口与联系人）逐行填写后回传，**即一并覆盖 G1/G3/G4/G5 所需信息**——请勿遗漏该文档 |
| G7 | **K 线交付排期**（**最高优先级**） | K1 workspace key 签发链未交付期间，CSI 核心业务链（商机到达 → 智能体承接 → 执行交付）在真实环境**完全不可运行**——智能体的 LLM 调用无任何通道（我方按冻结契约实现：runtime 凭证只能经 K1 daemon key 链，无旁路）。E1-E4 计量面即使就绪也无真实用量可计量。请给出 K1-K4 交付排期，或提供临时测试 key 通道供 beta 先打通业务链 |

**三、我方就绪度声明**

GatewayClient 全部 12 端点实现 + fail-fast 装配 + E4 增量游标对齐（今日 landed）+ 假网关 E2E 全绿——**端点地址 + 凭证（G1/G3）到位当天即可启动阶段 1 冒烟**，无需其它前置。



—— Agent Owner Console 团队