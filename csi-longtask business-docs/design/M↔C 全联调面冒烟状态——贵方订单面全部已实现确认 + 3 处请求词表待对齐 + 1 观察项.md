> 依据：evidence-scenario-sweep-2026-09-07.md（订单面 21 端点 / 入站 16 路由×22 事件 / W1 三端点 全量冒烟）。

---

**主题：M↔C 全联调面冒烟状态——贵方订单面全部已实现确认 + 3 处请求词表待对齐 + 1 观察项**

Marketplace 团队各位好：

发函前我方按"统一收集、统一回函"原则，对 M↔C 全部集成场景做了一轮全量真实冒烟。结论：**除网关计费面（另函）与 K 线外，双方联调面已基本全通**。详情如下。

**一、贵方订单面（场景二~九）全部端点已实现——确认**

我方以 c2m- 凭证对贵方订单面约 21 个端点逐一探针：**全部返回结构化业务响应**（order/settlement/dispute/cancel/spec-change 不存在的 404 业务错误、参数校验 400、成功路径 200/201）——即贵方场景二~九服务端已全部就绪。具体可达面：task 详情（200 含完整对象）、workspaces/{id}/orders 与 settlements（200 []）、revision-negotiation/start（201 成功）、以及全部订单子资源端点的路由与业务校验层（404/400 结构化）。

**二、3 处请求词表 drift（实测坐实，请确认后我方对齐）**

| # | 端点 | 贵方实测要求 | 我方当前发送 |
|---|---|---|---|
| D1 | `revision-negotiation/{id}/decide` | `decision` ∈ **A/B/C/D** | `option` = append_revision/… |
| D2 | `cancel-requests/{id}/respond` | `response` ∈ accept/reject/counter_proposal | `decision` |
| D3 | `cancel-requests/{id}/auto-resolve` | `outcome` ∈ accept_partial_settlement/reject_cancel | `resolution` |

我方按贵方实测词表对齐即可（非契约文本问题，以贵方实现为准）。请确认上述三点与我方理解一致；确认后我方落地修改（对账接入时一并验证）。

**三、1 个观察项（请贵方确认）**

`revision-negotiation/start` 对**不存在的 order**（我方用假 UUID 探针）仍返回 **201 创建成功**（negotiation id `033b4135-b1a3-4ddc-9215-59db87ff17cf`，order `1111…`）——疑似缺 order 存在性前置校验。请确认贵方是否有意为之（如 mock/无校验口径）；该残留记录请贵方清理或告知无需处理。

**四、我方承接面状态（供贵方推送时参考）**

1. **M→C webhook 16 条路由全部就绪**：我方以贵方信封形态（§8.3 data 容器）对 16 端点 × 全部 event_type 自签冒烟——路由/信封解析/type 白名单全链工作，其中 `settlement.completed`/`settlement.appeal_period_closed` 两条最小 body 全链 200 `processed`，`opportunity.pushed` 已由贵方真实重推闭账（07:27 processed/200）；其余事件类型待贵方真实场景推送时逐类走完整业务校验；
2. **W1 partner 档案 API 三端点 200 实测**（workspaces 列表/详情/org 列表）——贵方可随时拉取。

**五、我方后续动作预告**

贵方确认 D1-D3 词表后，我方将对账接入面与取消/修订协商发送面统一对齐（属既有契约修订项）；真实订单流转（贵方大厅现有任务选标推进）即可触发场景三~九逐类闭环。

—— Agent Owner Console 团队