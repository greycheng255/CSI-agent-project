# M 侧核对请求：契约回写状态同步（三处待确认）

> **发起方**：Marketplace（M 侧）
> **日期**：2026-09-07
> **事由**：我方持有契约副本停在 **2026-09-05 17:52 版**（103KB），而贵方 09-06 函件承诺/声称的回写均晚于该时点。请同步最新版契约副本，并书面确认以下三处回写状态。

---

## 一、待核对的三处回写

1. **§9.1 push 接收开关两分支行为**
   - 09-06 函件第二部分承诺："该开关语义为我方契约 §9.1 未写明的实现细节，我方将按 §7.1 回写契约文档（§9.1 补 push 接收条件与两分支行为说明）"。
   - 我方 09-05 版 §9.1 处理逻辑仍为旧四步（入账 → `(workspace_id, marketplace_task_id)` 查重 → 静默跳过 `deduplicated: true` → 创建商机记录），**无** `receive_platform_push` 开关与"开启 = 幂等投影 200 / 关闭或未注册 = 200 静默消费"两分支说明。
   - 请确认：是否已回写；如已回写请以最新版为准同步。

2. **§10.1 金额单位（整数元）**
   - M 侧回函（09-07）已确认**口径 A：全链统一整数元（int，人民币元取整）**，覆盖 `price_cny` / `budget_range.min / max` / 结算 `amount_cny` / 里程碑 `amount` / 仲裁 `partial_settlement` 金额；字段名保留 `*_cny`。
   - 我方 09-05 版 §10.1 及相关金额字段**无任何单位说明**。
   - 请确认：贵方按 §7.1 发起的金额口径变更是否已回写（含字段清单与单位注记）。

3. **§21.4 `workspace_avatar_url` 口径**
   - 09-06 函件第一部分声称："契约权威文档 employer-integration-api.md **§21.4 已同步回写**"（升级为解析后可渲染地址：站点相对 `/api/avatars/<sig>/<key>`，签名 `<sig>` 为 storage key 的确定性 HMAC 且**无 TTL**）。
   - 但我方 09-05 版 §21.4 表格仍是**旧文案**："Console 存储原值：对象存储 URL 或站点相对路径；渲染级绝对地址以 W1 详情返回的 `avatar_url` 为准"——与新口径**文字矛盾**。
   - 请确认：最新版 §21.4 的最终文字口径（两种口径 M 侧实现均兼容，但契约文字必须唯一）。

## 二、已确认并入项（无需动作，供对齐）

1. **§21 Workspace 档案发现（W1/W2/W3）已完整并入** 09-05 版：§21 全节（动机 / 鉴权通用约定 / W1 三端点 / W2 生命周期事件 / W3 席位快照 / 白名单硬边界）+ §2.2 端点清单三行（`GET /v1/partner/workspaces`、`GET /v1/partner/workspaces/{workspace_id}`、`POST /v1/webhooks/workspace/changed`）。
2. M 侧承接端点（W2）与消费侧（W1 Pull 兜底 + 事件投影）均已实现，与 §21 契约一致。

## 三、请求清单

1. 同步最新版 `employer-integration-api.md` 完整副本（联调群直发或只读路径均可）；
2. 对上述三处逐项书面确认状态（已回写 / 计划回写 + 时点）；
3. 如贵方另有其它在途回写项（如 §10.1 之外单位相关的字段清单），请一并列出，M 侧同步核对实现。

---

*M 侧联调联系人：（略）；本文档随仓库 `csi-longtask business-docs/design/` 同步。*
